endpoint = "{{ rebuilderd_server }}"
signup_secret = "{{ rebuilderd_signup_secret }}"

[build]
timeout = 86400 # 24 hours
max_bytes = 10485760 # 10 MiB

[diffoscope]
timeout = 600 # 10 minutes
max_bytes = 10485760 # 10 MiB

[backend."fedora"]
path = "/usr/libexec/rebuilderd/rebuilder-fedora.sh"
